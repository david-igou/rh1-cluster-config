---
apiVersion: automationcontroller.ansible.com/v1beta1
kind: AutomationController
metadata:
  name: aap-prod
  namespace: aap-prod
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    environment: prod
    app: ansible-automation-platform
spec:
  # Create a route for web UI access
  create_preload_data: true
  route_tls_termination_mechanism: Edge
  loadbalancer_port: 80
  loadbalancer_protocol: http
  
  # External PostgreSQL for Production (recommended)
  # Create secret 'aap-postgres-config' with connection details
  # Uncomment for external database:
  # postgres_configuration_secret: aap-postgres-config
  
  # Admin credentials - operator will auto-generate password
  # Password will be stored in secret: <instance-name>-admin-password
  admin_user: admin
  
  # Production sizing - HA with multiple replicas
  web_replicas: 3
  task_replicas: 3
  
  web_resource_requirements:
    requests:
      cpu: 2000m
      memory: 8Gi
    limits:
      cpu: 4000m
      memory: 16Gi
  
  task_resource_requirements:
    requests:
      cpu: 2000m
      memory: 8Gi
    limits:
      cpu: 4000m
      memory: 16Gi
  
  postgres_resource_requirements:
    requests:
      cpu: 1000m
      memory: 4Gi
    limits:
      cpu: 2000m
      memory: 8Gi
  
  redis_resource_requirements:
    requests:
      cpu: 500m
      memory: 2Gi
    limits:
      cpu: 1000m
      memory: 4Gi
  
  # Storage for projects
  projects_persistence: true
  projects_storage_size: 50Gi
  projects_storage_access_mode: ReadWriteMany
  
  # Enable garbage collection
  garbage_collect_secrets: true
  
  # Image pull policy
  image_pull_policy: IfNotPresent
  
  # Production-grade settings
  task_args:
    - '--worker-class=gevent'
    - '--workers=4'

